version: 0.2

phases:
  build:
    commands:
      - echo "Preparing backend for deployment..."
      # You can add tests or other steps here if needed

artifacts:
  files:
    - '**/*'
  base-directory: backend
