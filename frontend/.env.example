# Frontend environment variables
# These are VITE_ prefixed and safe to set (they become public in the bundle)
# The actual secrets (TMDB_TOKEN etc.) live in the root .env, loaded by the backend only.

VITE_API_BASE_URL=http://localhost:5000
